/**
 * Object that defines routes within the application. \
 * These route keys and values can be used for navigation and route matching.
 */
export const routes = {
  main: "/*",
  home: "/home",
}

/**
 * Object that defines redirects within the application. \
 * Redirects allow mapping one route to another for navigation purposes.
 */
export const redirects = {
  root: "/",
  home: "/home",
}
